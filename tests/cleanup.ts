/**
 * Stub cleanup script — Phase 0.5.
 * Real implementation lands when there's an actual orphan to clean.
 */
console.log("manual cleanup for now: see notes/test-orphans.log");
console.log("automated cleanup lands in Phase 1");
process.exit(0);
